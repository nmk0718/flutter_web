name: flutter_web
description: flutter_web

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  get: 4.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

